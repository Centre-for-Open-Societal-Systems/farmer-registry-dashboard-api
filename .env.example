DATABASE_URL=postgresql://postgres:postgres@localhost:5432/farmer_registry_db
API_V1_STR=/api/v1
PROJECT_NAME="Farmer Registry Dashboard API"
# JSON list of origins allowed by CORS. Browsers should not call this API directly.
ALLOWED_ORIGINS=["http://localhost:3000"]
# National unit counts per level for coverage rates (JSON). Levels left out are reported as null.
GEO_LEVEL_TOTALS={"woredas": 1138}
